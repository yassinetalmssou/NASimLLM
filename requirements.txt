# NASimLLM — direct dependencies
#
# PyTorch with CUDA must be installed separately (adjust cu121 if needed):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ----- NASim core -----
gymnasium>=0.26
numpy<2.0.0
networkx>=2.4
matplotlib>=3.1
pyyaml>=5.3
prettytable>=0.7

# ----- LLM4Teach -----
transformers>=4.40
accelerate>=0.26
bitsandbytes>=0.43
huggingface-hub>=0.20
safetensors
sentencepiece
tqdm

# ----- Training & logging -----
tensorboard>=2.2

# ----- Analysis (notebooks: rq1/rq2/rq3) -----
pandas
scipy
jupyter
